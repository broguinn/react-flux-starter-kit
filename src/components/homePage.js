'use strict';

const React = require('react');

const Home = React.createClass({
  render: () => (
    <div className='jumbotron'>
      <h1>Pluralsite Jerks</h1>
      <p>React, React Router, and Flux for whatever I guess</p>
    </div>
  )
});

module.exports = Home;
